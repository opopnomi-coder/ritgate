<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for MyGate
  - Production (onrender.com): HTTPS only, system CAs
  - Local dev (localhost, emulator, LAN): cleartext allowed
  - Default: HTTPS only
-->
<network-security-config>

    <!-- Production Render backend — HTTPS only -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">onrender.com</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>

    <!-- Local development — cleartext allowed -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Default: all other traffic must use HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>
