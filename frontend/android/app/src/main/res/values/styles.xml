<resources xmlns:tools="http://schemas.android.com/tools">

  <!-- Base app theme — edge-to-edge, no action bar, gesture nav friendly -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Status bar / nav bar colors (transparent for edge-to-edge) -->
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
    <!-- Light status bar icons (dark icons on light background) -->
    <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
    <!-- Light nav bar icons -->
    <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    <!-- Draw behind system bars (edge-to-edge) -->
    <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
    <!-- Enforce nav bar contrast — false for edge-to-edge gesture nav -->
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    <!-- Enforce status bar contrast -->
    <item name="android:enforceStatusBarContrast" tools:targetApi="29">false</item>
    <!-- Fit system windows — false so React Native handles insets -->
    <item name="android:fitsSystemWindows">false</item>
    <!-- EditText background -->
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <!-- Primary color -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- Window background -->
    <item name="android:windowBackground">@color/splashscreen_background</item>
    <!-- Gesture navigation: allow swipe-back from edges -->
    <item name="android:windowIsTranslucent">false</item>
  </style>

  <!-- Splash screen theme — shown before React Native loads -->
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
    <item name="android:statusBarColor">@color/splashscreen_background</item>
    <item name="android:navigationBarColor">@color/splashscreen_background</item>
    <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
    <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
  </style>

</resources>
